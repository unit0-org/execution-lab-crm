// Match the icon-button height so a row's action cell keeps the same
// height whether or not it holds a menu — rows stay aligned.
export const actionSlotStyle = {
  display: 'flex', alignItems: 'center', justifyContent: 'flex-end',
  minHeight: '32px'
}
