import { space } from '@/ui/tokens/space'

export const sidebarRowStyle = {
  display: 'flex',
  alignItems: 'center',
  gap: space[2],
  position: 'relative',
}

export const chipLinkStyle = {
  flex: 1,
  minWidth: 0,
  overflow: 'hidden',
  textDecoration: 'none',
  color: 'inherit',
  position: 'relative',
  zIndex: 1,
}
