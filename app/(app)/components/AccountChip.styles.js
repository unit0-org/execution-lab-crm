import { space } from '@/ui/tokens/space'

export const sidebarRowStyle = {
  display: 'flex',
  alignItems: 'center',
  gap: space[2],
}

export const chipLinkStyle = {
  flex: 1,
  minWidth: 0,
  textDecoration: 'none',
  color: 'inherit',
}
