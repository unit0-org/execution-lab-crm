// Ranked by conversion, so the table is not sortable — re-sorting it by
// anything else would undo the one thing it is for.
export const columns = [
  { label: 'Event' },
  { label: 'Type' },
  { label: 'Reg / att' },
  { label: '→ Client' }
]
