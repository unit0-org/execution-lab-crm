// How many events the ranking shows. The card names the cap in its
// footnote whenever there are more, so a short table never reads as
// "these are all the events we ran".
export const TOP_EVENTS = 5
