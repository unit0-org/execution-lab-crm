// The actions an automation can run, plus the config field each needs.
export const ACTIONS = [
  { value: 'send_email', label: 'send an email', config: 'template' },
  { value: 'create_task', label: 'create a task', config: 'task' }
]
