// Bar-chart dimensions (px). Geometry, canvas, skeleton and empty state
// all derive from these so the reserved size is identical — no layout
// shift between loading, empty and loaded.
export const CHART_HEIGHT = 180
export const CHART_TOP = 16
export const CHART_FOOTER = 24
