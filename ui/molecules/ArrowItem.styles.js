import { color } from '../tokens/color'
import { space } from '../tokens/space'

export const rowStyle = {
  display: 'flex', gap: space[3], alignItems: 'flex-start'
}

export const arrowStyle = { color: color.warmth.cold, flexShrink: 0 }
