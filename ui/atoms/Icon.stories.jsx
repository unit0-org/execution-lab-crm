import { Icon } from './Icon'

const meta = {
  title: 'Atoms/Icon',
  component: Icon,
  args: { name: 'plus' }
}

export default meta

export const Default = {}

export const Large = { args: { size: 32 } }
