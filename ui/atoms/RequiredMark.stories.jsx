import { RequiredMark } from './RequiredMark'

// Hidden from screen readers — the input's own required conveys it.
const meta = {
  title: 'Atoms/RequiredMark',
  component: RequiredMark
}

export default meta

export const Default = {}
