import { Link } from './Link'

const meta = {
  title: 'Atoms/Link',
  component: Link,
  args: { href: '#', children: 'View contact' }
}

export default meta

export const Default = {}

export const NewTab = { args: { newTab: true } }
