import { Avatar } from './Avatar'

const meta = {
  title: 'Atoms/Avatar',
  component: Avatar,
  args: { name: 'Ada Lovelace' }
}

export default meta

export const Initials = {}

export const Photo = { args: { src: '/portal-logo.png' } }

export const Large = { args: { size: 72 } }

export const Nameless = { args: { name: '' } }
