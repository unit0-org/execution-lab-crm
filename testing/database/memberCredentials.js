// The password portal-member factories seed by default. Comfortably over the
// CRM's own minimum, so a test that wants a too-short password has to say so.
export const MEMBER_PASSWORD = 'e2e-Memb3r-Pass!';
